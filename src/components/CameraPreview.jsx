const CameraPreview = () => {
  return (
    <div className="bg-black text-white rounded-xl h-64 flex items-center justify-center mb-6">
      <p>📸 Camera Preview (AI Module)</p>
    </div>
  );
};

export default CameraPreview;
